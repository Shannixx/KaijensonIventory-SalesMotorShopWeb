@model KaijensonIventory_SalesMotorShopWeb.Models.ServiceJob
@using System.Globalization
@{
    ViewData["Title"] = "Create Service Job";
    var services = ViewBag.ServicesList as List<KaijensonIventory_SalesMotorShopWeb.Models.Service> ?? new List<KaijensonIventory_SalesMotorShopWeb.Models.Service>();
    var ph = CultureInfo.GetCultureInfo("en-PH");
}
<div class="page-header">
    <div>
        <h1>Create Service Job</h1>
        <p>Record a new service job for a customer.</p>
    </div>
</div>

<div class="card form-card">
    <div class="card-body">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3 small"></div>

            <div class="mb-3">
                <label asp-for="ServiceId" class="form-label"></label>
                @if (services.Any())
                {
                    <div class="d-flex flex-wrap gap-2 mb-2" role="group" aria-label="Quick service selection">
                        @foreach (var s in services)
                        {
                            <button type="button" class="btn btn-sm btn-outline-secondary quick-service-btn" data-service-id="@s.ServiceId">@s.ServiceName</button>
                        }
                    </div>
                }
                <select asp-for="ServiceId" id="serviceSelect" class="form-select">
                    <option value="">-- Select Service --</option>
                    @foreach (var s in services)
                    {
                        <option value="@s.ServiceId" data-price="@s.ServicePrice.ToString(CultureInfo.InvariantCulture)" selected="@(Model.ServiceId == s.ServiceId)">
                            @s.ServiceName — ₱@s.ServicePrice.ToString("N2", ph)
                        </option>
                    }
                </select>
                <span asp-validation-for="ServiceId" class="text-danger small"></span>
                @if (!services.Any())
                {
                    <div class="form-text">No services defined yet. Add one under the Service tab first.</div>
                }
            </div>

            <div class="mb-3">
                <label for="servicePriceDisplay" class="form-label">Service Price</label>
                <input type="text" class="form-control" id="servicePriceDisplay" value="" readonly placeholder="-- Select a service --" />
                <div class="form-text">Price comes automatically from the selected service.</div>
            </div>

            <div class="mb-3">
                <label asp-for="MechanicId" class="form-label"></label>
                <select asp-for="MechanicId" asp-items="ViewBag.MechanicId" class="form-select">
                    <option value="">-- Select Mechanic --</option>
                </select>
                <span asp-validation-for="MechanicId" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="CustomerName" class="form-label"></label>
                <input asp-for="CustomerName" class="form-control" />
                <span asp-validation-for="CustomerName" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label">Description</label>
                <textarea asp-for="Description" class="form-control" rows="2" placeholder="What work will be performed (e.g. Change Oil)?"></textarea>
                <span asp-validation-for="Description" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="AmountReceived" class="form-label">Amount Received (₱)</label>
                <input asp-for="AmountReceived" class="form-control" type="number" step="0.01" min="0" value="0" />
                <span asp-validation-for="AmountReceived" class="text-danger small"></span>
                <div class="form-text">Payment status is computed automatically: Unpaid or Paid. Amount received cannot exceed the service price.</div>
            </div>

            <div class="form-actions">
                <input type="hidden" asp-for="SubmissionToken" />
                <button type="submit" class="btn btn-app-primary" id="createBtn">
                    <span>Create Service Job</span>
                </button>
                <a asp-action="Index" class="btn btn-app-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        (function () {
            var select = document.getElementById('serviceSelect');
            var display = document.getElementById('servicePriceDisplay');
            if (!select || !display) return;

            var fmt = new Intl.NumberFormat('en-PH', { style: 'currency', currency: 'PHP' });

            function updatePrice() {
                var option = select.options[select.selectedIndex];
                var price = option ? parseFloat(option.getAttribute('data-price')) : NaN;
                if (!isNaN(price)) {
                    display.value = fmt.format(price);
                } else {
                    display.value = '';
                    display.placeholder = '-- Select a service --';
                }
            }

            select.addEventListener('change', updatePrice);

            document.querySelectorAll('.quick-service-btn').forEach(function (btn) {
                btn.addEventListener('click', function () {
                    select.value = this.getAttribute('data-service-id');
                    select.dispatchEvent(new Event('change'));
                });
            });

            // Prevent duplicate submissions
            var form = document.querySelector('form');
            var btn = document.getElementById('createBtn');
            form.addEventListener('submit', function () {
                btn.disabled = true;
                btn.innerHTML = '<span>Creating Service Job...</span>';
            });

            updatePrice();
        })();
    </script>
}
